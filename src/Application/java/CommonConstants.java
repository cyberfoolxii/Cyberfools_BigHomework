package Application.java;

import javafx.stage.Screen;

public interface CommonConstants {
    double MAX_SCREEN_WIDTH = Screen.getPrimary().getBounds().getWidth();
    double MAX_SCREEN_HEIGHT = Screen.getPrimary().getBounds().getHeight();
}
